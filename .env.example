# Public form endpoint generated by the newsletter provider. This value is
# embedded in the exported HTML and must never be an API key.
NEWSLETTER_FORM_ACTION=https://example.com/your-newsletter-form-endpoint
